#include <iostream>

int main(int argc, char **argv) {
    if(argc < 2)
        std::cerr << "Usage: " << argv[0] << " <file>" << std::endl;
    else if(argc == 2)
        // TODO: run file
    else
        // TOOD: run prompt

    return 0;
}